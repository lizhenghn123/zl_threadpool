dist: trusty
language: cpp

compiler:
    - gcc
      
before_install:
    - lsb_release -a

install:
    - 

after_success:
    - 

branches:
    only:
    - master

notifications:
  email: true

script:
    - make
